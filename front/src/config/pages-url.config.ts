class DASHBOARD {
  private root = '/admin';

  HOME = '/';
  COLLECTIONS = `/collections`;
  SETTINGS = `/settings`;

  ADMIN_PANEL = `${this.root}/panel`;
}

export const DASHBOARD_PAGES = new DASHBOARD();
